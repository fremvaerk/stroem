listen: "0.0.0.0:8080"

db:
  url: "postgres://stroem:stroem@localhost:5432/stroem"

log_storage:
  local_dir: /tmp/stroem-logs

workspace:
  type: folder
  path: /Users/ala/workspace/fremvaerk/stroem/workspace

worker_token: "dev-worker-token-change-in-production"

auth:
  jwt_secret: "dev-jwt-secret-change-in-production"
  refresh_secret: "dev-refresh-secret-change-in-production"
  initial_user:
    email: "admin@stroem.local"
    password: "admin"
